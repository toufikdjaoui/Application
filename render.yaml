services:
  - type: web
    name: mode-dz-backend
    env: python
    region: frankfurt
    plan: free
    buildCommand: "cd backend && pip install -r requirements.txt"
    startCommand: "cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: MONGODB_URL
        value: "mongodb+srv://toufikdjaoui:toufikdjaoui@cluster0.xycidxj.mongodb.net/mode_dz?retryWrites=true&w=majority"
      - key: JWT_SECRET_KEY
        value: "mode-dz-super-secret-jwt-key-production-2024"
      - key: ENVIRONMENT
        value: "production"
      - key: CORS_ORIGINS
        value: "https://your-netlify-app.netlify.app"
      - key: PYTHON_VERSION
        value: "3.11"
